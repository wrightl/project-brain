# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: project-brain
services:  
  app:
    language: dotnet
    project: ./ProjectBrain.AppHost/ProjectBrain.AppHost.csproj
    host: containerapp
