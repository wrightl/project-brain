'use client';

/**
 * Theme loading is handled by ThemeProvider. This component remains as a
 * stable mount point for layouts that already include it.
 */
export function ThemeInitializer() {
    return null;
}
