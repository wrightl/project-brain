name: Deploy (staging)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  
jobs:
  deploy:
    uses: ./.github/workflows/azd-deploy.yml
    with:
      deploy_environment: staging
    secrets: inherit
