name: Deploy (staging)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/azd-deploy.yml
    with:
      deploy_environment: staging
    secrets: inherit
